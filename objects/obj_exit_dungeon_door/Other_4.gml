// Rough door lock / unlock initialization
//with obj_enemy_abstract {
    //other.to_kill += is_must_kill ? 1 : 0;
//}
//show_debug_message(to_kill)

// Ensure depth is set correctly (behind instances but in front of terrain)
depth = 190;